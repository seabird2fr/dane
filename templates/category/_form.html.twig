{{ form_start(form) }}
    
   <div style="display:none">
    {{ form_row(form.shortname,{'label':'Nom abrégé','required':false}) }}
	</div>

    {{ form_row(form.longname,{'label':'Nom'}) }}

    <a class="btn btn-secondary" href="{{ path('category_index') }}"><i class="fas fa-arrow-circle-left"></i> {{'Retour à la liste'|trans}}</a>
    {% if is_granted('ROLE_TEACHER') %}
        <button class="btn btn-primary"><i class="fas fa-check"></i> {{ button_label|default('Sauver'|trans) }}</button>
    {% endif %}

{{ form_end(form) }}
