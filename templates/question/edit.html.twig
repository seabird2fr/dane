{% extends 'base.html.twig' %}

{% form_theme form with ['bootstrap_4_layout.html.twig', _self] %}

{% block title %}{{'Editer Question'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Editer Question'|trans}}</h1>

    {% if is_granted('ROLE_TEACHER') %}
        {{ include('question/_form.html.twig', {'button_label': 'Mise à jour'|trans}) }}
    {% endif %}

{% endblock %}

{% if is_granted('ROLE_TEACHER') %}
    {% block javascripts %}
        <script src="{{ asset('js/question.js') }}" type="text/javascript"></script>
    

            <script>
// ajout seabird

//$('#category_color').selectpicker();

      $('#question_color').selectpicker({
          //style: 'btn-info',
          //size: 4
      });

</script>

    {% endblock %}
{% endif %}
